{
  "name": "wintermaul-web",
  "version": "1.20.0",
  "private": true,
  "description": "Wintermaul Web multiplayer with per-player gold and synchronized matches",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "node server/server.js",
    "release:check": "node scripts/release.js check",
    "release:package": "node scripts/release.js package",
    "release:baseline": "node scripts/release.js baseline",
    "release:mark-uploaded": "node scripts/release.js mark-uploaded"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
